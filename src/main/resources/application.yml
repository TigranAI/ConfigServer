server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://D/Projects/ConfigServer/configs
          search-paths: {application}